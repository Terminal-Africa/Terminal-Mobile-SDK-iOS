import Foundation

/// Enum representing the different Http methods used to make API calls.
enum Method: String {
    case get = "GET"
    case post = "POST"
    case delete = "DELETE"
    case patch = "PATCH"
}
